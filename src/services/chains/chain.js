//import SecretService from "./secretService";
//import EvmSerive from "./evmSerive";
/*import { getFactory } from "../../wallet/helpers";

class ChainService {
  _chain;
  _factory;

  constructor(from, factory) {
    this._factory = factory;

    switch (from.type) {
      case "Cosmos": {
        this._chain = SecretService();
        break;
      }

      default: {
        this._chain = EvmSerive();
      }
    }

    return this;
  }

  async approve(...args) {
    return await this._chain.approve(...args);
  }

  send() {}

  mint() {}
}

export default (from, factory) => ChainService(from, factory);
export { ChainService };*/
